<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.samteladze.vzradio"
    android:versionCode="9"
    android:versionName="0.33" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/just_fish_logo_cut_72x72"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >

        <activity
            android:name="com.samteladze.vzradio.MainWrapperActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.samteladze.vzradio.MediaPlayerService"
            android:exported="false"
            android:label="Media Player" >
            <intent-filter>
                <action android:name="com.samteladze.vzradio.action.PLAY" />
            </intent-filter>
        </service>

    </application>

</manifest>